fun main()
{
    var a = 10.2
    val b = 10.8
    var result : Double

    result = a+b
    // we can use another operation ( +, -, *, /, %) mathematical
    println(result)

    println(a==b)
    // We can use another operation ( ==, !=, <, >, <=, =>) comparision

    a*=b
    // We can use another operations ( +=, -=, *=, /=, %=) assignment
    println(a)

    a++
    // We can use another operations ( ++, --) increment and decreament
    println(a)

}
